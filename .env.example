# Planning Poker - Environment Configuration
# Copy this file to .env and fill in your values

# Admin Login Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=<generate with: python generate_admin_password.py>

# Flask Secret Key (WICHTIG: Muss gesetzt sein, sonst gehen Sessions bei jedem Neustart verloren!)
# Generiere einen Key mit: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=<your-secret-key-here>

# Database Path (für Docker Volume)
# Lokal: planning_poker.db (default)
# Docker: /app/data/planning_poker.db
DB_PATH=planning_poker.db

# Flask Environment (production oder development)
FLASK_ENV=development

# Feature Configuration
# Einhorn Easter Egg beim Aufdecken der Karten aktivieren (true/false)
ENABLE_UNICORN=false

# Einhorn Anzeigedauer in Sekunden (nur relevant wenn ENABLE_UNICORN=true)
UNICORN_DISPLAY_SECONDS=3

# Zuschauer-Modus Feature aktivieren (true/false)
ENABLE_SPECTATOR_MODE=true

# ============================================================================
# AI Module Configuration
# ============================================================================

# OpenAI API Key (für Embeddings mit text-embedding-3-small)
# Generiere Key auf: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Ollama Configuration (für lokale Embeddings)
# Standard: http://localhost:11434
# OLLAMA_HOST=http://localhost:11434

# Default Embedding Provider (mock, openai, ollama, sentence_transformers)
# mock: Fake embeddings für Tests/Entwicklung
# sentence_transformers: Lokale Open Source (empfohlen, benötigt: pip install sentence-transformers)
# ollama: Lokale Open Source via Server (benötigt laufenden Ollama Server)
# openai: OpenAI API (benötigt OPENAI_API_KEY)
AI_EMBEDDING_PROVIDER=sentence_transformers

# OpenAI Model Selection (falls provider=openai)
# text-embedding-3-small: 1536 dimensions, günstig, schnell
# text-embedding-3-large: 3072 dimensions, teurer, genauer
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Ollama Model Selection (falls provider=ollama)
# nomic-embed-text: 270 MB, schnell, gute Qualität
# mxbai-embed-large: 669 MB, langsamer, beste Qualität
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# sentence-transformers Model Selection (falls provider=sentence_transformers)
# all-MiniLM-L6-v2: 80 MB, schnell, gute Balance (empfohlen)
# paraphrase-multilingual-MiniLM-L12-v2: 135 MB, mehrsprachig
# all-mpnet-base-v2: 420 MB, beste Qualität
# AI_SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2
# AI_DEVICE=cpu  # oder 'cuda' für GPU

# Chunking Strategy (story, fixed, sentence, paragraph)
# story: Story-aware Chunking (empfohlen für Planning Poker)
# fixed: Fixed-size chunks mit Überlappung
# sentence: Satz-basiertes Chunking
# paragraph: Absatz-basiertes Chunking
AI_CHUNKING_STRATEGY=story
